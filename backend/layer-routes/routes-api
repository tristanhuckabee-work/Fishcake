const router = require('express').Router();
const controller = require('../layer-controllers').ControllerAPI
// ----------------------------------------------------------------------------

router.get("/csrf/restore", controller.RestoreCSRF);

// ----------------------------------------------------------------------------
module.exports = router;